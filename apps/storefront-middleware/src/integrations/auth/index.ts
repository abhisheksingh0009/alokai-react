// TODO: Auth integration helpers
// - hashPassword(plain: string) → Promise<string>
// - verifyPassword(plain: string, hash: string) → Promise<boolean>
// - signToken(payload: { userId, email }) → string
// - verifyToken(token: string) → decoded payload or null
