// TODO: Auth routes — mounted at /api/auth
//
// POST /api/auth/register
//   - Receives: { email, password, firstName, lastName }
//   - Returns: { user, token }
//
// POST /api/auth/login
//   - Receives: { email, password }
//   - Returns: { user, token }
//
// POST /api/auth/logout
//   - Clears session / invalidates token
//
// GET  /api/auth/me
//   - Requires: Authorization header with Bearer token
//   - Returns: { user }
