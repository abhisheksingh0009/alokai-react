{
  "name": "my-alokai-store",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web & npm run dev --workspace=apps/storefront-middleware",
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:middleware": "npm run dev --workspace=apps/storefront-middleware",
    "build": "npm run build --workspaces"
  }
}
